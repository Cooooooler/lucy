name: Probe

on:
  push:
    branches:
      - feature/auth-login
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo hello
