/**
 * @coool/file-nest 公共入口（骨架占位）。
 * 存储驱动抽象（StorageDriver）、文件元数据（FileEntity）、DI 服务（FileService、FileModule）
 * 将于后续任务补充。当前仅导出版本号占位，供 workspace 识别与 typecheck 通过。
 */
export const FILE_NEST_VERSION = '0.1.0';
